$('.modal#addFolder<%=@folder.id%>ToSharedList').modal('toggle');

$('.modal#validationModal').replaceWith("<%= j render('shared/validation_modal', message: @message) %>")
$('.modal#validationModal').modal("toggle");

<% @folders.each do |folder| %>
  $('.modal#addFolder<%=folder.id%>ToSharedList form#new_shared_list').replaceWith("<%= j render('folder_shared_lists/form', folder: folder, folder_shared_list: FolderSharedList.new) %>")
<% end %>

$('#shared-list-nav-item a').attr("href", "<%=shared_list_path(current_user.shared_lists.initial.first)%>")
